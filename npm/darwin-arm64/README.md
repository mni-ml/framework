# @mni-ml/framework-darwin-arm64

macOS Apple Silicon (aarch64) native binary for `@mni-ml/framework`.

This package is installed automatically by `@mni-ml/framework` — you should not install it directly.
